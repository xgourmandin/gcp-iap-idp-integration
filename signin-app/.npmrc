# gcip-iap@0.1.x declares firebase@^8 as a peer dependency.
# Firebase v9+ provides a full v8-compatible compat layer (firebase/compat/*),
# so we bypass the peer resolution error with legacy-peer-deps.
legacy-peer-deps=true

